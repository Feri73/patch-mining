class Clazz {
    private void main() {
        int u;
        // is order of h and k maintained? (no. why?)
        // why if i use x in 2p.java in other places it will not become i==j && 5
        // what if i edit it with if(x&&5)
        x = h == k;
        if (x) {
            k = k + 1;
            b = u;
            k = func(h);
        }
    }
}