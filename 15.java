class Clazz {
    public static int main(int arr[], int key, int imin, int imax) {
        if(arr > key)
            return binarySearch1(arr,key);
        else if (arr < key)
            return binarySearch1(arr,key);
        arr = 2;
    }
}